# Migrations

Bu repo’da klasik RDBMS migration mekanizması görülmüyor.

## Observed (from repo)

- Persistence: PouchDB document store (`src/app/services/main.service.ts`)

## TODO (needs confirmation)

- Prod ortamında kalıcı storage ve schema evolution nasıl yönetiliyor?
  - PouchDB doc versioning / migration stratejisi varsa buraya eklenmeli.

